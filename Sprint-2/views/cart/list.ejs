<%- include('../partials/header.ejs', {cs: 'homepage'})%>


    <div class="container p-3">
        <h1>Cart List</h1>
        <br>
        <%if(books.length){ %>

            <table class="table">
                <!--different headers for the cart table-->
                <tr>
                    <th scope="col"></th>
                    <th scope="col"></th>
                    <th scope="col">Book Title</th>
                    <th scope="col">Price</th>
                </tr>
                


            <tbody>
            <%books.forEach(book=>{%>
                
                <tr>
                    <!--delete button-->
                    <td>
                        <form>
                            <button type="submit" class="btn btn-danger" formmethod="POST" formaction="/cart/delete/<%= book._id%>?_method=DELETE"><i class="fa fa-trash"></i></button>
                        </form>
                    </td>
                    <!--image section-->
                    <td><img src=""></td>
                    <!--book title-->
                    <td><a href="/books/<%=book.bookId.id%>"><p><%= book.bookId.title %></p></a></td>
                    <!--book price-->
                    <td><p>$<%= book.bookId.price%></p></td>
                        
                </tr>
        
            <% }) %>

            </tbody>
            </table>
            
        <%}%>
        <%if(techs.length){ %>

            <table class="table">

                    <tr>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col">Tech Title</th>
                        <th scope="col">Price</th>
                    </tr>

            <tbody>
            <%techs.forEach(tech=>{%>
                
                <tr>
                    <td><img src=""></td>
                    <td><a href="/techs/<%=tech.techId.id%>"><p><%= tech.techId.title %></p></a></td>
                </tr>
        
            <% }) %>

            </tbody>
            </table>
            
        <%} %>
        <%if(empty){%>
            <h2>There are no items to display</h2>
        <%} %>
    </div>

<%- include('../partials/footer.ejs')%>