<%- include('../partials/header.ejs', {cs: 'defaultstyle'})%>

<div class="container p-3">

    <h1>My Posts</h1>
    <%if(books.length) {%>
        <%books.forEach(book => {%>
            <p><%= book.author %></p>
            <p><%= book.price %></p>
            <p><%= book.condition %></p>
        <%})%>
    <%}%>

</div>

<%- include('../partials/footer.ejs')%>